# Environment (chứa secret — KHÔNG commit)
.env.local
.env*.local

# Node / build artifacts (không dùng cho site tĩnh)
node_modules/
.next/
out/
dist/
build/

# Hệ điều hành / editor
.DS_Store
Thumbs.db
*.log